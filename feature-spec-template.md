# Шаблон спецификации фичи — «Кадр»

> Копировать для каждой новой фичи перед `/gsd:plan-phase`. Заполнять конкретикой
> (числа, типы, коды ошибок), без абстракций.

## Фича: <название>

### User stories
- Как <роль>, я хочу <действие>, чтобы <результат>.

### Данные (если меняется БД)
- Новые/изменённые таблицы и поля (SQL-типы, CHECK, FK, индексы).
- RLS-политики (кто что видит/правит). **Обязательно при любой новой таблице.**

### API (если есть)
| Метод/путь | Тело | Ответ | Коды ошибок |
|---|---|---|---|
|  |  |  |  |

### UI
- Экраны iOS / web, состояния (пусто/загрузка/ошибка), тексты на русском.

### Логика
- Пошагово: проверки, лимиты, идемпотентность, фоновые задачи.

### Edge cases
- Перечислить и описать ожидаемое поведение.

### Соответствие 152-ФЗ
- Затрагивает ли ПДн? Согласие? Подписанные URL? Локализация? Retention?
- Любая работа с фото лиц — проверить запрет биометрии.

### Готовность
- [ ] database-architect: миграция + RLS
- [ ] backend-engineer: API/логика
- [ ] frontend-developer: UI
- [ ] security-agent: аудит 152-ФЗ/RLS пройден
- [ ] qa-reviewer: контракт и edge cases пройдены
