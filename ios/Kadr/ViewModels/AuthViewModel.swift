import Foundation
import Observation

// MARK: - AuthViewModel
// Не используется напрямую — логика перенесена в LoginView + AuthViewModelStore.
// Оставлен как пустышка, чтобы не сломать проект при случайной ссылке.
// Для OTP-логики используй AuthService напрямую из LoginView.
