import Foundation
import Observation

// MARK: - CreateEventViewModel
// Упрощённая логика перенесена напрямую в CreateEventView (@State свойства).
// Файл оставлен как заглушка — удалить или расширить при необходимости.
